<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- 锁屏快速录音：临时点亮屏幕 + 在锁屏之上显示 MainActivity -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 日历权限：用于静默写入日历事件（替代闹钟 Intent） -->
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>
    <!-- 存储权限（Android 12 及以下） -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     android:maxSdkVersion="32" />
    <application
        android:label="声物记"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:roundIcon="@mipmap/launcher_icon"
        android:theme="@style/NormalTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/NormalTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Phase 4：静态快捷方式 intent-filter（自定义 action，
                 切换图标包后 alias 接管路由，不会因 MainActivity DISABLED 失败）-->
            <intent-filter>
                <action android:name="com.shengwuji.app.ACTION_QUICK_RECORD"/>
                <action android:name="com.shengwuji.app.ACTION_QUICK_TEXT_NOTE"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <!-- 静态快捷方式：供第三方 App（微动手势等）发现 -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- ==================== Phase 4：图标包切换（activity-alias） ==================== -->

        <!-- 暖色图标包（免费） -->
        <activity-alias
            android:name=".IconWarm"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/launcher_icon_warm"
            android:roundIcon="@mipmap/launcher_icon_warm"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Phase 4：静态快捷方式 intent-filter（自定义 action，
                 切换图标包后 alias 接管路由，不会因 MainActivity DISABLED 失败）-->
            <intent-filter>
                <action android:name="com.shengwuji.app.ACTION_QUICK_RECORD"/>
                <action android:name="com.shengwuji.app.ACTION_QUICK_TEXT_NOTE"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />
        </activity-alias>

        <!-- 节日红图标包（Pro） -->
        <activity-alias
            android:name=".IconFestive"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/launcher_icon_festive"
            android:roundIcon="@mipmap/launcher_icon_festive"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Phase 4：静态快捷方式 intent-filter（自定义 action，
                 切换图标包后 alias 接管路由，不会因 MainActivity DISABLED 失败）-->
            <intent-filter>
                <action android:name="com.shengwuji.app.ACTION_QUICK_RECORD"/>
                <action android:name="com.shengwuji.app.ACTION_QUICK_TEXT_NOTE"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />
        </activity-alias>

        <!-- 极简白图标包（Pro） -->
        <activity-alias
            android:name=".IconMinimal"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/launcher_icon_minimal"
            android:roundIcon="@mipmap/launcher_icon_minimal"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Phase 4：静态快捷方式 intent-filter（自定义 action，
                 切换图标包后 alias 接管路由，不会因 MainActivity DISABLED 失败）-->
            <intent-filter>
                <action android:name="com.shengwuji.app.ACTION_QUICK_RECORD"/>
                <action android:name="com.shengwuji.app.ACTION_QUICK_TEXT_NOTE"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />
        </activity-alias>

        <!-- 无障碍服务：长按音量键触发录音 -->
        <service
            android:name=".VolumeKeyAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 闹钟广播接收器 -->
        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true"
            android:exported="false" />
        <!-- 闹钟停止按钮接收器 -->
        <receiver
            android:name=".AlarmStopReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <!-- 使用 package 标签直接声明包名（Android 11+ 推荐） -->
        <package android:name="com.openai.chatgpt" />
        <package android:name="com.deepseek.chat" />
        <package android:name="com.moonshot.kimichat" />
        <package android:name="com.tencent.mm" />

        <!-- 闹钟应用查询（保留兼容，Flutter 端已改用日历） -->
        <intent>
            <action android:name="android.intent.action.SET_ALARM"/>
        </intent>

        <!-- PROCESS_TEXT 查询 -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- HTTPS 查询（备用） -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
            <data android:host="chat.openai.com"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
            <data android:host="chat.deepseek.com"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
            <data android:host="kimi.moonshot.cn"/>
        </intent>
    </queries>
</manifest>
